// Stand-in for next/script. The product loads third-party widgets this way (a chat
// bot on the home page, for example). The prototype loads no outside scripts.
export default function Script() {
  return null;
}
